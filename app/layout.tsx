import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'
import { Header } from '@/components/header'
import { Footer } from '@/components/footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Clash Master - Deck Builder & Academy',
  description: 'Master Clash Royale with our ultimate deck builder and academy. Build meta decks, climb arenas, and become a champion.',
  keywords: 'Clash Royale, deck builder, meta decks, arenas, cards, strategy',
  authors: [{ name: 'Clash Master Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-clash-darker text-foreground min-h-screen`}>
        <Providers>
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </Providers>
      </body>
    </html>
  )
}